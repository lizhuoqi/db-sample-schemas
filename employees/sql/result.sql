select 'dept_emp' tab, count(1) rows_count from dept_emp union all
select 'dept_manager' tab, count(1) rows_count from dept_manager union all
select 'titles' tab, count(1) rows_count from titles union all
select 'salaries' tab, count(1) rows_count from salaries union all
select 'employees' tab, count(1) rows_count from employees union all
select 'departments' tab, count(1) rows_count from departments;